{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://skillgraph.dev/schemas/skill.v2.schema.json",
  "title": "Skill Graph Skill Frontmatter (v2)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "name",
    "description",
    "version",
    "type",
    "family",
    "scope",
    "owner",
    "freshness",
    "drift_check",
    "eval_artifacts",
    "eval_state",
    "routing_eval"
  ],
  "properties": {
    "schema_version": {
      "oneOf": [
        { "type": "integer", "const": 2 },
        { "type": "string", "pattern": "^[0-9]+$" }
      ]
    },
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-/:]*$"
    },
    "description": {
      "type": "string",
      "minLength": 20
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "type": {
      "type": "string",
      "enum": ["capability", "workflow", "router", "overlay"]
    },
    "family": {
      "type": "string"
    },
    "scope": {
      "type": "string",
      "enum": ["codebase", "reference", "portable"]
    },
    "owner": {
      "type": "string"
    },
    "freshness": {
      "type": "string",
      "format": "date"
    },
    "drift_check": {
      "type": "string",
      "format": "date"
    },
    "eval_artifacts": {
      "type": "string",
      "enum": ["none", "planned", "present"]
    },
    "eval_state": {
      "type": "string",
      "enum": ["unverified", "passing", "monitored"]
    },
    "routing_eval": {
      "type": "string",
      "enum": ["absent", "present"]
    },
    "stability": {
      "type": "string",
      "enum": ["experimental", "stable", "frozen", "deprecated"]
    },
    "license": {
      "type": "string"
    },
    "compatibility": {
      "type": "string"
    },
    "allowed-tools": {
      "type": "string"
    },
    "extends": {
      "type": "string"
    },
    "triggers": {
      "type": "array",
      "items": { "type": "string" }
    },
    "keywords": {
      "type": "array",
      "items": { "type": "string" }
    },
    "paths": {
      "type": "array",
      "items": { "type": "string" }
    },
    "routing_groups": {
      "type": "array",
      "items": { "type": "string" }
    },
    "relations": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "adjacent": {
          "type": "array",
          "items": { "type": "string" }
        },
        "boundary": {
          "type": "array",
          "items": { "type": "string" }
        },
        "verify_with": {
          "type": "array",
          "items": { "type": "string" }
        },
        "depends_on": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "grounding": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "domain_object",
        "grounding_mode",
        "truth_sources",
        "failure_modes",
        "evidence_priority"
      ],
      "properties": {
        "domain_object": { "type": "string" },
        "grounding_mode": {
          "type": "string",
          "enum": ["repo_specific", "universal", "hybrid"]
        },
        "truth_sources": {
          "type": "array",
          "items": { "type": "string" }
        },
        "failure_modes": {
          "type": "array",
          "items": { "type": "string" }
        },
        "evidence_priority": {
          "type": "string",
          "enum": ["repo_code_first", "general_knowledge_first", "equal"]
        }
      }
    },
    "portability": {
      "type": "object",
      "additionalProperties": false,
      "required": ["readiness", "targets"],
      "properties": {
        "readiness": {
          "type": "string",
          "enum": ["declared", "scripted", "verified"]
        },
        "targets": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["skill-md"]
          }
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "type": { "const": "overlay" }
        }
      },
      "then": {
        "required": ["extends"]
      }
    },
    {
      "if": {
        "properties": {
          "scope": { "const": "codebase" }
        },
        "required": ["scope"]
      },
      "then": {
        "required": ["grounding"]
      }
    }
  ]
}
